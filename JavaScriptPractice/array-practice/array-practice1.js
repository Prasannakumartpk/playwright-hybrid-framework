let students = ["Prasanna", "Pavan", "Avinash", "Vedhansh"];

// Access the Array
console.log(students); 

// length of Array
console.log(students.length);

// Access the single element in Array
console.log(students[2]); 

// Update the element in Array
students[2] = "Vamika"; 
console.log(students); 


/* let sports = ["Cricket", "Coco", "Vallyball"];

// Add one or more elements to the end of an array and return the length of the array.
sports.push("Basketball");
console.log(sports);

// Removes the last element from an Array and the returns the element.
sports.pop();
console.log(sports);

// Add one or more elements to the beginning of an array and return the length of the array.
sports.unshift("Kabadi");
console.log(sports);

// Removes the first element from an Array and the returns the element.
sports.shift();
console.log(sports); */

/* let cities = ["Hyderabad", "Mumbai", "Kerala",]
join();
// Creates and returns a new string by concatenating all of the elements in an array, separated by commas or a specified separator string.
let myCities = cities.join("");
console.log(myCities);

tostring();
// Returns a string representing the specified array and its elements.
let mycity = cities.toString();
console.log(cities);
console.log(mycity);

indexOf();
// Returns the first index at which a given element can be found in the array, or -1 if it is not present.
console.log(cities.indexOf("Mumbai"));

includes();
// Determines whether an array includes a certain value among its entries, returning true or false.
console.log(cities.includes("Andhra")); */


/* // Merges two or more arrays and returns a new array
let subjects = ["Maths", "Social", "Science"];
let language = ["Telugu", "Hindi", "English"];

let add = subjects.concat(language);
console.log(add);  */

/* //slice();
// Returns a shallow copy of a portion of an array into a new array.
let family = ["Prasanna", "Sarika", "Vamika", "Vedhansh", 34, 29, 4, 3];
let myFamily = family.slice(1,5);
console.log(myFamily);

//splice();
// Changes the contents of an array by removing or replacing existing elements and/or adding new elements.
let myNumber = [1, 2, 3, 4, 5, 6, 7, 8];
//let myNewNumber = myNumber.splice(0,3); // Removing
//let myNewNumber = myNumber.splice(3, 2, "Vicky"); // Replace
let myNewNumber = myNumber.splice(4,0,"Arun"); // Adding
console.log(myNumber); */





